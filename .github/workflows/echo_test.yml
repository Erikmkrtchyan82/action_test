name: Echo something

on:
  push:
    branches: [master]

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: first_test
        run: echo 'Hi there!'
  second:
    runs-on: ubuntu-latest
    steps:
      - name: second_test
        run: echo 'second test' >> file.txt; cat file.txt