name: Echo something

on:
  push:
    branches: [master]

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: first
      run: echo 'Hi there!'

  test1:
    runs-on: ubuntu-latest 
    steps:
      - name: Echo something
        run: echo 'something'